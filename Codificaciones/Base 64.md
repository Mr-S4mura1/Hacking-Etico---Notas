#Codificaciones #HackingEtico 

**Nota**: Para desencriptar un codigo de base64 podemos ultilizar el siguiente comando en la terminal para desencriptar. Este es el comando:

```bash
echo "HASH" | base64 -d
```

----
